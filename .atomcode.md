# CPF-Skills 项目指令

本仓库用于构建鸿蒙三方库适配与征文流程的上层 Skills。

## 总体原则

1. **官方 Skill 优先**：CPF-Flutter、CPF-RN、CPF-ApplicationTPC 等社区已经提供的能力，优先调用或编排，不在本仓库维护平行实现。
2. **单一职责**：发现候选、适配必要性判断、实际移植、文章整理、文章检查属于不同阶段；不要把全部逻辑塞进一个 Skill。
3. **事实与流程分离**：Skill 维护工作流；框架路由、仓库地址、活动规则等可变事实放入 `resources/` 或 Skill 的 `references/`。
4. **实时事实要验证**：最新版本、维护状态、是否已经适配、下载量/热度、当前仓库内容等不得仅凭模型记忆断言。
5. **不伪造证据**：没有检查过的仓库、没有运行过的代码、没有看到的截图、没有执行的测试，都不能写成“已验证”。
6. **不复制官方事实源**：如果官方文档或官方 Skill 已经是事实源，只保存路由和引用，不复制大段内容形成第二份易过期数据。
7. **Fixture 与真实证据隔离**：`fixture://...` 只允许出现在显式 `fixture_only=true` 的回归测试输入中；真实 qualification、validation、article material、manual confirmation 或 compliance 不得使用 fixture 证据。
8. **确定性门禁优先**：状态机、资格门禁、Validation、静态文章检查、完整 compliance 等能机器确定的规则必须由脚本计算，模型不得覆盖或新增隐藏门禁。

## Skill 结构

项目级 Skill 放在：

```text
.atomcode/skills/<skill-name>/SKILL.md
```

Skill 名只使用小写字母、数字、`-`、`_`。

复杂参考资料放入该 Skill 的 `references/`；跨 Skill 共享且经常变化的配置放入仓库根目录 `resources/`。

## 当前 Skill 边界

主链按以下职责拆分：

1. `thirdparty-library-discovery`：发现候选、资格初筛、官方判断与活动去重。
2. `harmony-contribution-orchestrator`：qualification gate、framework route、validation gate。
3. `harmony-article-writing`：真实素材整理、提纲、章节映射与局部润色；不得一键生成整篇参赛文章。
4. `harmony-article-check`：静态规则、Validation、外部指标、人工确认与发布状态检查。

新增能力时继续保持这些边界，不把已存在的框架官方移植能力复制进本仓库。

## 征文相关约束

当任务目标是生成、修改或检查本次鸿蒙三方库适配征文时：

- 文章必须基于真实适配材料，不虚构开发经历、运行结果或验证过程。
- 必须先完成对应中心仓/社区仓去重；没有完成强制去重时，不得断言该库符合征文资格。
- 技术上“需要鸿蒙适配”和“符合本次征文资格”必须分开判断。
- 进入 `ARTICLE_PREP` 前必须通过真实 implementation、build、demo、tests、HarmonyOS/OpenHarmony 实体设备运行和截图 Validation Gate。
- 最终文章中涉及代码托管平台时按活动要求使用 AtomGit 和 `https://atomgit.com` 链接；不得出现 GitCode 品牌或其链接。
- 活动合规规则以 `resources/article-rules.yaml` 为结构化单一事实源；框架社区名与组织链接以 `resources/frameworks.yaml` 为事实源。
- 原创性、AI 是否生成全部或大部分正文、重复率、CSDN 质量分、真机截图真实性等不能由语言模型自行宣称通过。
- 阅读量属于发布后指标，不得作为发布前文章门禁。

## 真实案例接入

初始化真实案例时优先使用：

```text
python3 scripts/evidence/init_real_case.py <framework> <candidate> <output-dir>
```

初始化结果必须默认阻塞：qualification 为 `NEEDS_OFFICIAL_CHECK`，Validation 六项为 `MISSING`，外部指标为空，人工确认均为 `NOT_PROVIDED`。只有真实证据补齐并通过现有 deterministic gates 后才能继续推进。

## 修改规范

- 修改 Skill 时优先保持向后兼容；状态名、配置 key 发生破坏性变化时同步提升 schema/version 并更新 README。
- 新增框架前先补 `resources/frameworks.yaml` 路由，再实现该框架流程。
- 未审计官方 Skill 名称时不要凭记忆写入配置；允许只登记官方 Skills 仓库并标记待审计。
- `experimental` 框架不得在文档中描述为“已完整支持”。
- 测试 fixture 必须显式标记并保持 `publishable=false`；不得通过删除 fixture 标记把测试数据伪装成真实案例。
- 增加真实案例时必须保存可追溯到真实仓库、构建日志、测试结果、实体设备与截图的 evidence 引用。
