name: CPF-RN Official Skills Audit

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "scripts/ci/fetch-cpf-rn-skills.sh"
      - ".github/workflows/cpf-rn-official-skills-audit.yml"

permissions:
  contents: read

concurrency:
  group: cpf-rn-official-skills-audit-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit-official-skills:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Audit CPF-RN official Skills
        shell: bash
        run: bash scripts/ci/fetch-cpf-rn-skills.sh

      - name: Upload CPF-RN Skill audit
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: cpf-rn-official-skills-audit
          path: .artifacts/official-skills/cpf-rn/
          if-no-files-found: error
